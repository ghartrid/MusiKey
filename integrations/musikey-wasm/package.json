{
  "name": "musikey-wasm",
  "version": "1.0.0",
  "description": "MusiKey WebAssembly module — runs in any browser or WASM runtime",
  "main": "dist/musikey-wasm.js",
  "scripts": {
    "build": "tsc && node build.js",
    "test": "node dist/test.js"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
